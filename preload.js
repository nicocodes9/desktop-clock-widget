// preload.js - A script that runs before your web page content
// This script has access to Node.js APIs and can expose them to the renderer safely.
// For this simple clock, we don't need to expose anything, but it's good practice.
